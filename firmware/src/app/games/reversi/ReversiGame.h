#pragma once

#include <lvgl.h>

/**
 * JEV REVERSI — 人間 1 人 対 AI のリバーシ。6×6（短い）と 8×8（長め）。
 *
 * 入口 → 盤面の大きさ → 相手 → 石の色 → 対局 → 結果 までを LVGL の画面 1 枚の
 * 中身を作り替えて進める（人狼・探偵・エスパー・AI DUEL と同じ作り）。
 *
 * **ルール・合法手・反転・パス・勝敗はすべて端末**（設計一式の無改変のコア
 * `core/reversi_core.hpp` と `core/reversi_session.hpp`）。Jev は「置ける場所の
 * 一覧から 1 手を選ぶ」だけで、返ってきた手は端末でもう一度合法かどうかを確かめる。
 *
 * 相手は JEV / JEV PRO / CASUAL（GAS 経由）と 端末AI の 4 種。Jev 系は Wi-Fi が
 * つながっているときだけ選べる。通信が 12 秒で返らなければ本人が
 * 「もう一度 / 端末AIで続行 / カフェ」を選ぶ。**黙って相手を入れ替えない。**
 * いちど端末AIに切り替えた局は Jev に戻さない（設計書 12.3）。
 *
 * 1 手確定するたびに NVS `ct_rev/game` へ保存し、**保存に成功してから**盤面を
 * 確定表示にする（設計書 5.4）。電源が切れても「画面に出たのに残っていない手」は
 * 生まれない。待ったは無い。
 *
 * 盤面は 312×312・左上 (84,84)・6×6 は 52px / 8×8 は 39px（設計書 4.1）。
 * LVGL の部品を 64 個作らず、**1 つの部品の描画コールバックで盤ごと描く**
 * （内蔵メモリと LVGL タスクのスタック 8KB を食いつぶさないため）。
 * 対局の状態は画面を開いたときに PSRAM へ確保し、閉じるときに手放す。
 *
 * 詳しい取り決めは docs/REVERSI_PLAN.md（この実装の正）と
 * 参考データ/ゲーム4部作/COFFEE_TIME_JEV_REVERSI_Design_v1.0.md を参照。
 */
namespace reversi {

// ゲーム画面を作る（ui::push に渡す）。入口から結果までこの 1 枚で進む
lv_obj_t *createGameScreen();

// 開発用：今の場面をシリアルへ 1 行出す。
// 盤面は公開情報なので、そのまま出してよい（人狼の秘密とは違う）
void debugPrintPublicState();

}  // namespace reversi
